[pytest]
# WSR/ must be importable so `from app import app` works, and tests/ so the
# shared helpers in conftest.py can be imported by name. Without this the
# documented `cd WSR && pytest tests/ -v` fails at collection, because
# tests/__init__.py makes tests a package and hides conftest from a bare import.
pythonpath = . tests
testpaths = tests
